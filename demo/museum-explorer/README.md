# Museum Explorer

An interactive museum exhibition guide built as a standalone HTML/CSS/JS prototype.

Open `index.html` directly, or serve the repository root and open:

`http://127.0.0.1:4173/demo/museum-explorer/`

The interface includes program day selection, artwork filters, saved-event feedback, and a keyboard-dismissible artwork detail panel.
