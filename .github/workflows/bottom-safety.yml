name: Phone Safety QA

on:
  push:
    branches: [main]
    paths:
      - "demo/**"
      - "catalog/cases/**"
      - "src/components/device-preview/phone-shell.css"
      - "scripts/audit_demo_bottom_safety.mjs"
      - "scripts/audit_demo_screen_safety.mjs"
      - "package.json"
      - ".github/workflows/bottom-safety.yml"
  pull_request:
    branches: [main]
    paths:
      - "demo/**"
      - "catalog/cases/**"
      - "src/components/device-preview/phone-shell.css"
      - "scripts/audit_demo_bottom_safety.mjs"
      - "scripts/audit_demo_screen_safety.mjs"
      - "package.json"
      - ".github/workflows/bottom-safety.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: phone-safety-${{ github.ref }}
  cancel-in-progress: true

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install Playwright
        run: |
          npm install --no-save --no-package-lock playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Start local site
        run: |
          IMAGE2_UI_PORT=4174 npm run serve > /tmp/ui-case-server.log 2>&1 &
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:4174/library.html > /dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/ui-case-server.log
          exit 1
      - name: Audit bottom navigation safety
        run: npm run audit:bottom-safety
      - name: Audit status bars and screen balance
        run: npm run audit:screen-safety
      - name: Upload phone safety reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phone-safety-report
          path: |
            artifacts/visual-qa/bottom-safety
            artifacts/visual-qa/screen-safety
          if-no-files-found: error
          retention-days: 14
