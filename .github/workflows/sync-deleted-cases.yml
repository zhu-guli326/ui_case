name: Sync deleted case catalog

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/sync-deleted-cases.yml"

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Rebuild active case index
        run: node scripts/sync_case_index.mjs
      - name: Commit synchronized catalog
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add catalog/index.js
          if git diff --cached --quiet; then
            echo "Nothing to sync"
            exit 0
          fi
          git commit -m "chore: sync 19-case catalog after removals"
          git push origin main
